{
  "name": "colors-server",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "clean": "rm db.json && touch db.json",
    "start": "npm run clean && nodemon --watch './**/*.js' index.js"
  },
  "devDependencies": {
    "@apollo/datasource-rest": "6.0.0",
    "@apollo/server": "5.4.0",
    "graphql": "16.8.1",
    "lowdb": "7.0.1",
    "nodemon": "3.1.14"
  }
}
